@inherits LayoutComponentBase
@inject ILocalizationService L
@implements IDisposable

<MudThemeProvider Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1" Dense="true">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start"
                       OnClick="@ToggleDrawer" />
        <MudIcon Icon="@Icons.Material.Filled.CalendarMonth" Class="ml-2" />
        <MudText Typo="Typo.h6" Class="ml-2">@L["AppTitle"]</MudText>
        <MudSpacer />
        <LanguageSelector />
        <AuthorizeView>
            <Authorized>
                <MudText Typo="Typo.body2" Class="mr-2 d-none d-sm-block">@context.User.Identity?.Name</MudText>
                <MudButton Variant="Variant.Text" Color="Color.Inherit" Href="authentication/logout"
                           StartIcon="@Icons.Material.Filled.Logout">
                    @L["SignOut"]
                </MudButton>
            </Authorized>
            <NotAuthorized>
                <MudButton Variant="Variant.Text" Color="Color.Inherit" Href="authentication/login"
                           StartIcon="@Icons.Material.Filled.Login">
                    @L["SignIn"]
                </MudButton>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>

    <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        <NavMenu />
    </MudDrawer>

    <MudMainContent Style="display: flex; flex-direction: column; min-height: calc(100vh - 48px);">
        <div class="pa-4" style="flex: 1;">
            @Body
        </div>
        <Footer />
    </MudMainContent>
</MudLayout>

<MudScrollToTop>
    <MudFab Color="Color.Secondary" StartIcon="@Icons.Material.Filled.ArrowUpward" />
</MudScrollToTop>

<div id="blazor-error-ui">
    @L["UnhandledError"]
    <a href="" class="reload">@L["Reload"]</a>
    <a class="dismiss" aria-label="Dismiss error">
        <MudIcon Icon="@Icons.Material.Filled.Close" />
    </a>
</div>

@code {
    private bool _drawerOpen = true;

    private readonly MudTheme _theme = new MudTheme
    {
        PaletteLight = new PaletteLight
        {
            Primary = "#6b6c6b",
            PrimaryContrastText = "#FFFFFF",
            Secondary = "#cf9b67",
            SecondaryContrastText = "#FFFFFF",
            AppbarBackground = "#5a5b5a",
            AppbarText = "#FFFFFF",
            DrawerBackground = "#FAFAFA",
            DrawerText = "#424242",
            DrawerIcon = "#757575",
            Background = "#F5F5F5",
            BackgroundGray = "#EEEEEE",
            Surface = "#FFFFFF",
            Success = "#43A047",
            Warning = "#cf9b67",
            Error = "#E53935",
            Info = "#6b6c6b",
            TextPrimary = "#212121",
            TextSecondary = "#757575",
            TextDisabled = "#BDBDBD",
            Divider = "#E0E0E0",
            DividerLight = "#EEEEEE"
        },
        PaletteDark = new PaletteDark
        {
            Primary = "#8b8c8b",
            Secondary = "#dfab77",
            AppbarBackground = "#1E1E1E",
            DrawerBackground = "#272727",
            Background = "#121212",
            Surface = "#1E1E1E"
        },
        Typography = new Typography
        {
            Default = new Default
            {
                FontFamily = new[] { "Roboto", "Helvetica", "Arial", "sans-serif" }
            }
        }
    };

    protected override async Task OnInitializedAsync()
    {
        Console.WriteLine("[MainLayout] OnInitializedAsync started");
        L.OnLanguageChanged += OnLanguageChanged;
        Console.WriteLine("[MainLayout] Subscribed to OnLanguageChanged");
        await L.InitializeAsync();
        Console.WriteLine($"[MainLayout] InitializeAsync complete, CurrentCulture: {L.CurrentCulture}");
        Console.WriteLine($"[MainLayout] AppTitle value: {L["AppTitle"]}");
        // Force re-render after translations are loaded
        StateHasChanged();
    }

    private void OnLanguageChanged()
    {
        Console.WriteLine($"[MainLayout] OnLanguageChanged called, CurrentCulture: {L.CurrentCulture}");
        // Use InvokeAsync to ensure StateHasChanged is called on the UI thread
        InvokeAsync(() =>
        {
            Console.WriteLine("[MainLayout] Calling StateHasChanged");
            StateHasChanged();
            Console.WriteLine("[MainLayout] StateHasChanged completed");
        });
    }

    private void ToggleDrawer() => _drawerOpen = !_drawerOpen;

    public void Dispose()
    {
        L.OnLanguageChanged -= OnLanguageChanged;
    }
}
